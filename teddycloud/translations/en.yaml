---
configuration:
  log.level:
    name: Log level
    description: 0=off - 6=verbose
  
  log.color:
    name: Colored log
    
network:
  80/tcp: The webserver port (Not used for Ingress)
  443/tcp: The Toniebox server (direct access for flashed Boxes)
